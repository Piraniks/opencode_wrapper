services:
  opencode_wrapper:
    build: .
    container_name: opencode_wrapper
    runtime: sysbox-runc
    tty: true
    stdin_open: true
    init: true
    environment:
      OPENCODE_DISABLE_AUTO_UPDATE: "1"
      OPENCODE_CONFIG: /opencode_local/opencode.json
      UID: "${UID:-1000}"
      GID: "${GID:-1000}"
      XDG_CONFIG_HOME: /opencode_config
      XDG_DATA_HOME: /tmp/opencode-data
      XDG_CACHE_HOME: /tmp/opencode-cache
      XDG_STATE_HOME: /opencode_data/opencode/state
    volumes:
      - "${OPENCODE_WORKDIR:-$PWD}:/workdir"
      - ./check-tools.sh:/usr/local/bin/check-tools
      - ./config:/opencode_config/opencode
      - ./data:/opencode_data/opencode
      - ./opencode.local:/opencode_local:ro
